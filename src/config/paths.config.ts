export const enum userpaths {
  DEFAULT_PATH = "/",
  FETCH_USER_BY_ID = "/:id",
}

export const enum teampaths {
  DEFAULT_PATH = "/",
  FETCH_TEAM_BY_ID = "/:id",
}

export const enum prefixes {
  USER_PREFIX = "/user",
  TEAM_PREFIX = "/team",
}
